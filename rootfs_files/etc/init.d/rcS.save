#!/bin/sh

mount -t proc none /proc
mount -t sysfs none /sys
mount -t devtmpfs devtmpfs /dev

echo "Starting Network..."

ifconfig lo  updhcpc -i eth0

echo "Starting telnet..."

telnetd -l /bin/sh

echo "Boot Successful!"


