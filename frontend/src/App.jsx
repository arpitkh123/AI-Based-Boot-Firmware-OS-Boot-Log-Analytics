// This file is not used. The app is bootstrapped via main.jsx → router.jsx.
// Kept as an empty module to avoid import errors if referenced elsewhere.

export default function App() {
  return null;
}
